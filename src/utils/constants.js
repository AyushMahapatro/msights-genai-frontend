/**
 * Application Constants
 * ----------------------
 * Central place for static values.
 * Keeps magic strings out of components.
 */

export const APP_NAME = "MSights GenAI";

export const MAX_UPLOAD_FILES = 5;

export const DEFAULT_MODEL = "gemini-1.5-pro";

export const ROUTES = {
  HOME: "/",
  LOGIN: "/login",
};

export const STORAGE_KEYS = {
  AUTH: "app_auth",
  THEME: "app_theme",
  MODEL: "app_selected_model",
};
